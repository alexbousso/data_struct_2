#include "../array.h"
#include "test_utils.h"
#include <string>

#define ASSERT_INVALID_INPUT(arg) ASSERT_THROWS(InvalidInput, arg)
#define ASSERT_INDEX_OUT_OF_BOUNDS(arg) ASSERT_THROWS(IndexOutOfBounds, arg)
#define ASSERT_SIZES_DO_NOT_MATCH(arg) ASSERT_THROWS(SizesDoNotMatch, arg)

std::string location;

int main() {

	return 0;
}
